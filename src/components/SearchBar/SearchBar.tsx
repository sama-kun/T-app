import "./SearchBar.css"; 

export const SearchBar = (
) => {
    return (
      <input
        className="search"
        type="text"
        placeholder="Search"
      />
    );
  };

export default SearchBar;